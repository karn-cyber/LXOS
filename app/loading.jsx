import LoadingOverlay from '@/components/ui/loading-overlay';

export default function Loading() {
  return <LoadingOverlay message="Loading LX Management OS..." subtitle="Hang tight while we bring everything up." />;
}
